import { ListElementsDirective } from './list-elements-directive';

describe('ListElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListElementsDirective();
    expect(directive).toBeTruthy();
  });
});
