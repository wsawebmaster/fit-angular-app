import { OnBlurTriggerValidationDirective } from './on-blur-trigger-validation-directive';

describe('OnBlurTriggerValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new OnBlurTriggerValidationDirective();
    expect(directive).toBeTruthy();
  });
});
